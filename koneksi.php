<?php
    $host = "localhost"; // Nama hostnya
    $username = "root"; // Username
    $password = ""; // Password (Isi jika menggunakan password)
    $database = "data_pegawaii"; // Nama databasenya
    $conn= mysqli_connect($host, $username, $password, $database); 
    // Koneksi ke MySQL
?>
